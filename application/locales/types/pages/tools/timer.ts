import { FAQItem } from "../../faq";

export interface TimerTranslations {
  title: string;
  description: string;
  keywords: string[];
  minutes: string;
  seconds: string;
  start: string;
  pause: string;
  reset: string;
  timeUp: string;
  faqList: FAQItem[];
}
